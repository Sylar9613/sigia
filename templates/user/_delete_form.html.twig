<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('¿Está seguro que desea eliminar este elemento?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger {{ button_class|default('btn-block') }}" style="{{ button_style|default('margin-top: 10px; margin-bottom: 10px;') }}"><i class="fa fa-fw fa-trash"></i>&nbsp;Eliminar</button>
</form>

