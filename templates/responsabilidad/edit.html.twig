{% extends 'base.html.twig' %}

{% block title %}Editar responsabilidad administrativa{% endblock %}

{% block body %}
    <br/>
    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">{{ routes[0] }}</li>
        <li class="breadcrumb-item">{{ routes[1] }}</li>
        <li class="breadcrumb-item">
            <a class="normal" href="{{ path('responsabilidad_index') }}">{{ routes[2] }}</a>
        </li>
        <li class="breadcrumb-item activo">{{ routes[3] }}</li>
    </ol>
    <h1>Editar responsabilidad administrativa</h1>
    {{ include('admin/errors.html.twig') }}
    {{ include('responsabilidad/_form.html.twig', {'button_label': 'Actualizar', 'button_class': 'btn-warning', 'button_fa': 'fa-refresh'}) }}

    {{ include('responsabilidad/_delete_form.html.twig') }}
    <a class="btn btn-secondary btn-block" href="{{ path('responsabilidad_index') }}">Regresar</a>
{% endblock %}
