{% extends 'base.html.twig' %}

{% block title %}Crear nueva responsabilidad{% endblock %}

{% block body %}
    <br/>
    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">{{ routes[0] }}</li>
        <li class="breadcrumb-item">{{ routes[1] }}</li>
        <li class="breadcrumb-item">
            <a class="normal" href="{{ path('responsabilidad_index') }}">{{ routes[2] }}</a>
        </li>
        <li class="breadcrumb-item activo">{{ routes[3] }}</li>
    </ol>
    <h1>Crear nueva responsabilidad</h1>
    {{ include('admin/errors.html.twig') }}
    {{ include('responsabilidad/_form.html.twig') }}

    <a href="{{ path('responsabilidad_index') }}" class="btn btn-secondary btn-block" style="margin-top: 5px;">Atr&aacute;s</a>
{% endblock %}
