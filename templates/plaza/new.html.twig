{% extends 'base.html.twig' %}

{% block title %}Crear nuevas plazas{% endblock %}

{% block body %}
    <br/>
    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">{{ routes[0] }}</li>
        <li class="breadcrumb-item">
            <a class="normal" href="{{ path('plaza_index') }}">{{ routes[1] }}</a>
        </li>
        <li class="breadcrumb-item activo">{{ routes[2] }}</li>
    </ol>
    <h1>Crear nuevas plazas</h1>
    {{ include('admin/errors.html.twig') }}
    {{ include('plaza/_form.html.twig') }}

    <a href="{{ path('plaza_index') }}" class="btn btn-secondary btn-block" style="margin-top: 5px;">Atr&aacute;s</a>
{% endblock %}
