{% extends 'base.html.twig' %}

{% block title %}Editar {{ nivel.nombre }}{% endblock %}

{% block body %}
    <br/>
    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">{{ routes[0] }}</li>
        <li class="breadcrumb-item">
            <a class="normal" href="{{ path('nivel_index') }}">{{ routes[1] }}</a>
        </li>
        <li class="breadcrumb-item activo">{{ routes[2] }}</li>
    </ol>
    <h1>Editar nivel escolar</h1>
    {{ include('admin/errors.html.twig') }}
    {{ include('nivel/_form.html.twig', {'button_label': 'Actualizar', 'button_class': 'btn-warning', 'button_fa': 'fa-refresh'}) }}

    {{ include('nivel/_delete_form.html.twig') }}

    <a class="btn btn-secondary btn-block" href="{{ path('nivel_index') }}">Regresar</a>
{% endblock %}
